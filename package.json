{
  "name": "roadmap-timeliner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "npm run build --workspace @roadmap-timeliner/core && npm run build --workspace @roadmap-timeliner/export && npm run build --workspace @roadmap-timeliner/document-service && npm run build --workspace @roadmap-timeliner/agent-kit && npm run build --workspace @roadmap-timeliner/cli && npm run build --workspace @roadmap-timeliner/mcp && npm run build --workspace @roadmap-timeliner/desktop",
    "test": "npm run build --silent && vitest run && npm run test:mcp --silent",
    "test:watch": "vitest",
    "test:mcp": "node scripts/mcp-smoke.mjs",
    "roadmap": "npm run build --silent && node packages/cli/dist/index.js",
    "mcp": "npm run build --silent && node packages/mcp/dist/index.js",
    "desktop": "npm --workspace @roadmap-timeliner/desktop run start",
    "desktop:roadmap": "npm --workspace @roadmap-timeliner/desktop run start -- roadmap.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
